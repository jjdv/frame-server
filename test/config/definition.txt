Test scope

1. geting config from selected directories
- all valid paths
- 2 invalid paths
- 2 config data sets with different data types
* path works normal
* fs.existsSync returns true only for a selected correct path
* require returns one of defined configs for the selected path
* correct config merge is checked

2. geting config from cli
- valid relative path
- valid absolute path
- invalid path
- 2 config data sets with different data types
* path works normal
* fs.existsSync returns true only for a selected correct path
* require returns one of defined configs for the selected path
* correct config merge is checked
